/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from '@solana/kit';
import { SVM_ALM_CONTROLLER_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const SYNC_RESERVE_DISCRIMINATOR = 8;

export function getSyncReserveDiscriminatorBytes() {
  return getU8Encoder().encode(SYNC_RESERVE_DISCRIMINATOR);
}

export type SyncReserveInstruction<
  TProgram extends string = typeof SVM_ALM_CONTROLLER_PROGRAM_ADDRESS,
  TAccountController extends string | AccountMeta<string> = string,
  TAccountControllerAuthority extends string | AccountMeta<string> = string,
  TAccountReserve extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountController extends string
        ? ReadonlyAccount<TAccountController>
        : TAccountController,
      TAccountControllerAuthority extends string
        ? ReadonlyAccount<TAccountControllerAuthority>
        : TAccountControllerAuthority,
      TAccountReserve extends string
        ? WritableAccount<TAccountReserve>
        : TAccountReserve,
      TAccountVault extends string
        ? ReadonlyAccount<TAccountVault>
        : TAccountVault,
      ...TRemainingAccounts,
    ]
  >;

export type SyncReserveInstructionData = { discriminator: number };

export type SyncReserveInstructionDataArgs = {};

export function getSyncReserveInstructionDataEncoder(): FixedSizeEncoder<SyncReserveInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', getU8Encoder()]]),
    (value) => ({ ...value, discriminator: SYNC_RESERVE_DISCRIMINATOR })
  );
}

export function getSyncReserveInstructionDataDecoder(): FixedSizeDecoder<SyncReserveInstructionData> {
  return getStructDecoder([['discriminator', getU8Decoder()]]);
}

export function getSyncReserveInstructionDataCodec(): FixedSizeCodec<
  SyncReserveInstructionDataArgs,
  SyncReserveInstructionData
> {
  return combineCodec(
    getSyncReserveInstructionDataEncoder(),
    getSyncReserveInstructionDataDecoder()
  );
}

export type SyncReserveInput<
  TAccountController extends string = string,
  TAccountControllerAuthority extends string = string,
  TAccountReserve extends string = string,
  TAccountVault extends string = string,
> = {
  controller: Address<TAccountController>;
  controllerAuthority: Address<TAccountControllerAuthority>;
  reserve: Address<TAccountReserve>;
  vault: Address<TAccountVault>;
};

export function getSyncReserveInstruction<
  TAccountController extends string,
  TAccountControllerAuthority extends string,
  TAccountReserve extends string,
  TAccountVault extends string,
  TProgramAddress extends Address = typeof SVM_ALM_CONTROLLER_PROGRAM_ADDRESS,
>(
  input: SyncReserveInput<
    TAccountController,
    TAccountControllerAuthority,
    TAccountReserve,
    TAccountVault
  >,
  config?: { programAddress?: TProgramAddress }
): SyncReserveInstruction<
  TProgramAddress,
  TAccountController,
  TAccountControllerAuthority,
  TAccountReserve,
  TAccountVault
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SVM_ALM_CONTROLLER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    controller: { value: input.controller ?? null, isWritable: false },
    controllerAuthority: {
      value: input.controllerAuthority ?? null,
      isWritable: false,
    },
    reserve: { value: input.reserve ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.controller),
      getAccountMeta(accounts.controllerAuthority),
      getAccountMeta(accounts.reserve),
      getAccountMeta(accounts.vault),
    ],
    data: getSyncReserveInstructionDataEncoder().encode({}),
    programAddress,
  } as SyncReserveInstruction<
    TProgramAddress,
    TAccountController,
    TAccountControllerAuthority,
    TAccountReserve,
    TAccountVault
  >);
}

export type ParsedSyncReserveInstruction<
  TProgram extends string = typeof SVM_ALM_CONTROLLER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    controller: TAccountMetas[0];
    controllerAuthority: TAccountMetas[1];
    reserve: TAccountMetas[2];
    vault: TAccountMetas[3];
  };
  data: SyncReserveInstructionData;
};

export function parseSyncReserveInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedSyncReserveInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      controller: getNextAccount(),
      controllerAuthority: getNextAccount(),
      reserve: getNextAccount(),
      vault: getNextAccount(),
    },
    data: getSyncReserveInstructionDataDecoder().decode(instruction.data),
  };
}
